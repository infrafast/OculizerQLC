# Core dependencies
numpy>=1.21.0,<2.0.0
scipy>=1.7.0

# Audio processing
librosa>=0.9.2
sounddevice>=0.4.6
soundfile>=0.11.0
pydub

# Machine learning
scikit-learn>=1.0.0
joblib>=1.1.0
torch==2.11.0
torchaudio==2.11.0
torchvision==0.26.0

# EfficientAT is installed separately with --no-deps. Its historical package
# metadata pins PyTorch 1.13, which is unavailable for Python 3.11 on Apple
# Silicon. See the installation command in README.md.

# Lighting control
pyserial>=3.5
PyDMXControl

# Terminal UI
windows-curses>=2.3.0; sys_platform == 'win32'
