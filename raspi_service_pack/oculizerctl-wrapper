#!/usr/bin/env bash
set -euo pipefail
config=/etc/oculizer/deployment.json
[[ -r $config ]] || { echo "oculizerctl: missing $config" >&2; exit 1; }
repo=$(python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))["repository"])' "$config")
socket_path=$(python3 -c 'import json,sys; print(json.load(open(sys.argv[1]))["control_socket"])' "$config")
exec "$repo/.venv/bin/python" "$repo/oculizerctl.py" --socket "$socket_path" "$@"
